## What changed

<!-- One or two sentences. What does this PR do? -->

## Why

<!-- The problem this solves. If it fixes an item in docs/improvements.md,
     reference the number and remove it from that file. -->

## How it was verified

<!-- How do you know it works? Emulator walkthrough, new tests, manual steps.
     "It builds" is not verification. -->

## Checklist

- [ ] `npm run format` has been run
- [ ] `npm run lint` is clean
- [ ] `npm test` passes
- [ ] `npm run build` succeeds
- [ ] Docs updated if this changes a Firestore field (`docs/data-model.md`) or a
      command (`docs/commands.md`)
- [ ] Any `eslint-disable` added names its rule and explains why
